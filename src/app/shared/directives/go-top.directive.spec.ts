import { GoTopDirective } from './go-top.directive';

describe('GoTopDirective', () => {
  it('should create an instance', () => {
    const directive = new GoTopDirective();
    expect(directive).toBeTruthy();
  });
});
